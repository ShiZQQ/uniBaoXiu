import { LogType, SpeedLog } from "../../../../core/src";
import { WebConfig } from '../../aegis';
export declare const createLine: (value: string, label: string) => string;
/**
 * 获取请求类型，判断是接口请求，还是静态资源请求
 * @param config
 * @param contentType
 * @param url
 * @returns
 */
export declare const getRequestType: (config: WebConfig, contentType: string | undefined, url: string) => any;
export declare const getLogLevel: (isAjaxError: boolean, isErr: boolean) => LogType.API_RESPONSE | LogType.AJAX_ERROR | LogType.RET_ERROR;
/**
 * 从 performance 中获取接口真实耗时
 * @param config
 * @param defaultValue
 * @returns
 */
export declare const getPerformanceEntryByUrl: (config: WebConfig, defaultValue: {
    url: string;
    duration: number;
    type: 'static' | 'fetch';
    status: number;
    method: string;
}) => SpeedLog;
